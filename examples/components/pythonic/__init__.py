"""
Custom Pythonic Components — Local Component Library

Place custom component definitions here. These components extend ifckit's
built-in component system with project-specific geometry generators.

To use components from this directory, import them directly or register
them with the component registry.
"""
